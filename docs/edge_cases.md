1. 在线模型回退至本地模型时，能正常输出打牌推荐；若此时满足立直条件，能正常输出包含立直前瞻的打牌推荐。
2. 本地模型恢复至在线模型时，能正常输出打牌推荐；若此时满足立直条件，能正常输出包含立直前瞻的打牌推荐。
3. 游戏掉线重连后，能正常输出打牌推荐；若此时满足立直条件，能正常输出包含立直前瞻的打牌推荐。
4. 游戏掉线重连且在线模型回退至本地模型时，能正常输出打牌推荐；若此时满足立直条件，能正常输出包含立直前瞻的打牌推荐。